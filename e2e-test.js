const fetch = require('node-fetch');

async function runTests() {
  const BASE_URL = 'http://localhost:3000'; // Make sure the server is running, or we can use the vercel URL
  console.log("Starting E2E Tests...");
}
runTests();
